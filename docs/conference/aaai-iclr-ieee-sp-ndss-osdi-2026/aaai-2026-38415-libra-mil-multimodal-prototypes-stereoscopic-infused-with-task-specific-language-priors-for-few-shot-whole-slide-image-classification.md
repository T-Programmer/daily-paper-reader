---
title: "Libra-MIL: Multimodal Prototypes Stereoscopic Infused with Task-specific Language Priors for Few-shot Whole Slide Image Classification"
title_zh: "Libra-MIL: 融合任务特定语言先验的多模态原型立体化用于少样本全切片图像分类"
authors: "Zhenfeng Zhuang, Fangyu Zhou, Liansheng Wang"
date: 2026-03-17
pdf: "https://ojs.aaai.org/index.php/AAAI/article/download/38415/42377"
tags: ["query:path-agent"]
score: 6.0
evidence: 融合语言先验的多模态原型用于病理分类
tldr: 全切片图像分类中，LLM生成的实例描述缺乏医学知识，导致偏差。本文提出Libra-MIL，通过构建任务特定病理实体原型，融合语言先验，增强可解释性和少样本性能。实验证明该方法有效提升分类准确率并减少偏差。
source: AAAI-2026-Accepted
selection_source: conference_retrieval
motivation: LLM生成的实例描述存在医学知识偏差，影响病理分类性能。
method: 构建任务特定病理实体原型，融合语言先验和多实例学习，形成立体化原型表示。
result: 在少样本设定下，Libra-MIL在WSI分类中取得更高准确率和更好可解释性。
conclusion: 融入语言先验的原型学习是提升病理MIL性能的有效方向。
---

## Abstract
While Large Language Models (LLMs) are emerging as a promising direction in computational pathology, the substantial computational cost of giga-pixel Whole Slide Images (WSIs) necessitates the use of Multi-Instance Learning (MIL) to enable effective modeling. A key challenge is that pathological tasks typically provide only bag-level labels, while instance-level descriptions generated by LLMs often suffer from bias due to a lack of fine-grained medical knowledge. To address this, we propose that constructing task-specific pathological entity prototypes is crucial for learning generalizable features and enhancing model interpretability. Furthermore, existing vision-language MIL methods often employ unidirectional guidance, limiting cross-modal synergy. In this paper, we introduce a novel approach, Multimodal Prototype-based Multi-Instance Learning, that promotes bidirectional interaction through a balanced information compression scheme. Specifically, we leverage a frozen LLM to generate task-specific pathological entity descriptions, which are learned as text prototypes. Concurrently, the vision branch learns instance-level prototypes to mitigate the model's reliance on redundant data. For the fusion stage, we employ the Stereoscopic Optimal Transport (SOT) algorithm, which is based on a similarity metric, thereby facilitating broader semantic alignment in a higher-dimensional space. We conduct few-shot classification and explainability experiments on three distinct cancer datasets, and the results demonstrate the superior generalization capabilities of our proposed method.

---

## 论文详细总结（自动生成）

# 论文《Libra-MIL》详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **研究背景**：全切片图像（WSI）在计算病理学中至关重要，但其像素级分辨率极高，因此通常采用多实例学习（MIL）框架进行弱监督建模。近年来，大型语言模型（LLM）被引入病理学，用于生成文本描述以辅助视觉特征学习，形成视觉-语言MIL（VLMIL）方法。
- **核心问题**：
  - LLM生成的实例级描述由于缺乏细粒度医学知识，往往存在偏差，且仅依赖类名的手动提示缺乏病理先验，判别力有限。
  - 现有VLMIL方法多采用单向指导（如文本→视觉），限制了跨模态的双向协同，导致语义对齐不充分，难以充分利用多模态信息。
  - 在少样本场景下，标注数据稀缺，模型泛化能力面临挑战。
- **整体含义**：为了解决上述问题，论文提出一种融合任务特定语言先验的多模态原型立体化MIL框架（Libra-MIL），旨在通过双向交互和原型学习提升少样本WSI分类的性能和可解释性。

## 2. 论文提出的方法论
- **核心思想**：构建视觉和文本双模态原型，通过最优传输（Optimal Transport）实现双向立体融合，同时利用袋级和实例级语言先验指导特征学习。
- **关键技术细节**：
  1. **任务特定语言先验生成（T-LPG）**：
     - 设计两种提示：实例级（描述病理实体形态）和袋级（描述全切片形态模式），输入冻结LLM（如GPT-4o）生成文本描述。
     - 使用冻结文本编码器（如CONCH中的文本编码器）将描述嵌入为文本原型 z_ins, z_bag。
  2. **双原型多模态学习器（DPM）**：
     - **视觉原型**：可学习的K_v个视觉原型 P_v。
     - **文本原型**：来自实例级语言先验的K_t个文本原型 P_t。
     - **相似度计算**：分别计算每个视觉实例与视觉原型、文本原型的余弦相似度，得到两个相似度矩阵 S_v, S_t。
  3. **立体化融合（Stereoscopic Infusion via SOT）**：
     - 将S_v和S_t视为三维空间中的两个切片，通过最优传输融合。
     - 定义跨模态代价矩阵C（基于视觉与文本原型的余弦距离）。
     - 估计边缘分布μ, ν（通过对相似度矩阵平均并softmax得到）。
     - 求解熵正则化最优传输问题，得到传输计划T，最终融合相似度 ˜S_j = S_v(j,:) T S_t(j,:)^⊤。
  4. **语义聚合（SA）**：
     - 用融合相似度重新加权视觉实例，生成融合特征 X_fused。
     - 使用袋级语言先验 z_bag 作为查询，通过交叉注意力聚合得到袋级表示 H。
     - 经过分类头得到最终logits。
- **训练**：使用交叉熵损失，Sinkhorn算法实现可微的最优传输求解。

## 3. 实验设计
- **数据集**：
  - TCGA-RCC（肾细胞癌，925张WSI，3个亚型）
  - TCGA-NSCLC（非小细胞肺癌，1052张WSI，2个亚型）
  - CAMELYON16（乳腺癌淋巴结转移，399张WSI，二分类）
- **场景**：少样本学习，设置1-shot、4-shot、16-shot。
- **评估指标**：准确率（ACC）、曲线下面积（AUC）、F1分数，五折交叉验证取均值和标准差。
- **对比方法**：
  - 传统MIL：Max Pooling, ABMIL, TransMIL, CLAM (SB/MB), DSMIL
  - 原型MIL：PAMIL
  - VLMIL：TOP-MIL, FOCUS, Libra-MIL（本文方法）
- **额外实验**：
  - 不同视觉编码器（Phikon, GigaPath, UNI, CONCH）
  - 不同LLM（Gemini 2.5, Qwen3, OpenAI-o4mini, Claude4-Sonnet, GPT-4o）
  - 视觉原型数量K_v的消融（2,4,6,8,10,12）
  - 可解释性可视化（原型对应组织形态、注意力热图）

## 4. 资源与算力
- **未明确说明**：论文附录C.1、C.2、C.5提及实现细节和超参数，但未明确给出所用GPU型号、数量、训练时长等信息。仅提及使用PyTorch框架，视觉编码器为CONCH（冻结），文本编码器采用对应配置。

## 5. 实验数量与充分性
- **实验数量**：大量实验，包括：
  - 三个数据集 × 三种shot设置 × 9种基线方法（共81个主要结果）
  - 消融实验：模块消融（表2）、视觉编码器替换（表3）、LLM对比（图3）、原型数量影响（表4）
  - 可解释性分析（图4、图5）
- **充分性**：实验设计较为充分：
  - 公平性：所有方法使用相同的数据划分和预处理；五折交叉验证减少偶然性；统计显著性检验（p<0.05）。
  - 覆盖面：涵盖了不同数据规模、不同shot数、不同基线类型，验证了各模块贡献。
  - 客观性：结果公开，代码开源。
- **不足**：仅在公开数据集上测试，未涉及真实临床环境；未与其他基于CLIP的方法（如MI-Zero）比较；缺少端到端训练的大规模实验。

## 6. 论文的主要结论与发现
- Libra-MIL在三个数据集、所有少样本设置下均优于现有SOTA方法，平均ACC提升2.49%，AUC提升2.61%，F1提升2.15%。
- 每个模块（实例先验、视觉原型、SOT融合、袋级查询）都贡献了性能提升，缺少任何模块都会导致性能下降。
- 视觉-文本特征空间的对齐程度对性能有显著影响（不同视觉编码器差异大），CONCH由于是视觉-语言联合训练模型，效果最佳。
- GPT-4o生成的文本先验优于其他LLM，但Libra-MIL对LLM的选择具有鲁棒性。
- 视觉原型数量存在最优值（K_v=6），过多或过少均会降低性能。
- 多模态原型具有可解释性：视觉原型对应有诊断意义的组织形态（如神经束、有丝分裂像、角化珠），注意力图聚焦于临床相关区域；梯度分析显示视觉和文本原型贡献相当。

## 7. 优点
- **方法创新**：首次在VLMIL中引入任务特定语言先验（袋级+实例级）和双模态原型，并通过最优传输实现双向立体融合，突破了单向指导的限制。
- **少样本能力强**：通过原型学习和语言先验，有效缓解了数据稀疏问题，在1-shot下仍取得显著提升。
- **可解释性好**：原型可视化和注意力热图提供了直观的病理学解释，增强模型透明度。
- **实验严谨**：多数据集、多shot、多基线、多消融，且开源代码，便于复现和后续研究。

## 8. 不足与局限
- **特征空间依赖**：模型性能高度依赖于视觉编码器与文本编码器的跨模态对齐程度，使用非联合训练的编码器（如Phikon、GigaPath）时性能明显下降，表明融合机制仍有改进空间。
- **算力消耗未披露**：文中未说明训练所需GPU型号、数量和时间，不利于评估实际部署成本。
- **应用限制**：仅针对少样本分类任务验证，未探索更复杂的任务（如生存分析、多标签分类）；CAMELYON16数据集较小（399例），二分类任务可能不具有代表性。
- **病理实体描述质量**：LLM生成的实例级描述可能仍包含噪声或与任务无关的信息，未详细分析描述质量对结果的影响。
- **无大规模数据验证**：仅在公开数据集上测试，未在更大规模的临床数据上验证泛化性。
- **消融实验仅在一个数据集**：模块消融（表2）仅在TCGA-RCC上进行，未在其他数据集重复验证。

（完）
